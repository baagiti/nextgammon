# NEXTGAMMON

A cyberpunk deckbuilder backgammon game — classic backgammon rules layered with mutation cards, boss protocols, and a 44-stage campaign.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
3. Build for production:
   `npm run build`
